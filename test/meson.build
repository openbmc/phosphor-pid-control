sensor_manager_unittest = executable('sensor_manager_test',
  'sensor_manager_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('sensor_manager_test', sensor_manager_unittest)

sensor_pluggable_unittest = executable('sensor_pluggable_unittest',
  'sensor_pluggable_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('sensor_pluggable_test', sensor_pluggable_unittest)


sensor_host_unittest = executable('sensor_host_unittest',
  'sensor_host_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('sensor_host_unittest', sensor_host_unittest)


util_unittest = executable('util_unittest',
  'util_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('util_unittest', util_unittest)

pid_zone_unittest = executable('pid_zone_unittest',
  'pid_zone_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('pid_zone_unittest', pid_zone_unittest)

pid_thermalcontroller_unittest = executable('pid_thermalcontroller_unittest',
  'pid_thermalcontroller_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('pid_thermalcontroller_unittest', pid_thermalcontroller_unittest)

pid_fancontroller_unittest = executable('pid_fancontroller_unittest',
  'pid_fancontroller_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('pid_fancontroller_unittest', pid_fancontroller_unittest)

pid_stepwisecontroller_unittest = executable('pid_stepwisecontroller_unittest',
  'pid_stepwisecontroller_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('pid_stepwisecontroller_unittest', pid_stepwisecontroller_unittest)

dbus_passive_unittest = executable('dbus_passive_unittest',
  'dbus_passive_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])
test('dbus_passive_unittest', dbus_passive_unittest)

dbus_active_unittest = executable('dbus_active_unittest',
  'dbus_active_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('dbus_active_unittest', dbus_active_unittest)

dbus_util_unittest = executable('dbus_util_unittest',
  'dbus_util_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('dbus_util_unittest', dbus_util_unittest)

sensors_json_unittest = executable('sensors_json_unittest',
  'sensors_json_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('sensors_json_unittest', sensors_json_unittest)

pid_json_unittest = executable('pid_json_unittest',
  'pid_json_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('pid_json_unittest', pid_json_unittest)

json_parse_unittest = executable('json_parse_unittest',
  'json_parse_unittest.cpp',
  include_directories : ['..'],
  install_dir: bindir,
  dependencies: [
    nlohmann_json,
    sdbusplus,
    systemd,
    phosphor_logging,
    threads,
    gtest,
    gmock,
  ],
  link_with: [
    libswampd_a,
  ])

test('json_parse_unittest', json_parse_unittest)
